export const PlatinumSponsor = ({ Sponsor }) => {
  return (
    <div className="h-10 w-full bg-slate-100 flex items-center justify-center sm:max-w-[900px] mx-auto">
      <p className="text-base font-semibold text-custom-gray">{Sponsor}</p>
    </div>
  );
};
